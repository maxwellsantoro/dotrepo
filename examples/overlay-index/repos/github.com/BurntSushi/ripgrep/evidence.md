# Evidence

- Imported repository identity, description, and maintainer handle from https://github.com/BurntSushi/ripgrep.
- Inferred the build command `cargo build --release` and the test command `cargo test` from the public Cargo project layout and common Rust repository conventions.
- `security_contact = "unknown"` is intentional in this example because the simplified overlay does not import a clearly documented private reporting channel from upstream materials.
- This is an overlay record, not a canonical maintainer-controlled record.
