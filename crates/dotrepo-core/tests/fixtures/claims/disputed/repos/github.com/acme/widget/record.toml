schema = "dotrepo/v0.1"

[record]
mode = "overlay"
status = "verified"
source = "https://github.com/acme/widget"

[record.trust]
confidence = "medium"
provenance = ["imported", "verified"]

[repo]
name = "widget"
description = "Verified overlay with disputed maintainer claim"
homepage = "https://github.com/acme/widget"
build = "cargo build --workspace"
test = "cargo test --workspace"
