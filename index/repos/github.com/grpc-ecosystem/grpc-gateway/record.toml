schema = "dotrepo/v0.1"

[record]
mode = "overlay"
status = "verified"
source = "https://github.com/grpc-ecosystem/grpc-gateway"
generated_at = "2026-07-06T18:45:09.207782Z"

[record.trust]
confidence = "high"
provenance = [
    "imported",
    "inferred",
    "verified",
]
notes = "Bootstrapped from `README.md`, `.github/workflows/ci.yml`, `Makefile`, and `go.mod`. Filled `repo.description` with inferred defaults. Inferred `repo.build` from `.github/workflows/ci.yml`. Imported `repo.test` from `Makefile`. Imported `repo.toolchain.min` from `go.mod`. This is an overlay bootstrap, not a maintainer-controlled canonical record. Preserved prior verified status: no previously present field regressed in this refresh."

[repo]
name = "gRPC-Gateway"
description = "gRPC to JSON proxy generator following the gRPC HTTP spec"
homepage = "https://grpc-ecosystem.github.io/grpc-gateway/"
license = "BSD-3-Clause"
visibility = "public"
languages = [
    "Go",
    "Starlark",
    "Makefile",
]
build = "go build ./..."
test = "make test"
topics = [
    "go",
    "grpc",
    "grpc-gateway",
    "rest-api",
    "restful-api",
    "swagger",
    "openapi",
]

[repo.toolchain]
min = "1.25.0"
ecosystem = "Go"

[docs]
root = "https://grpc.io/docs/"

[x.github]
archived = false
clone_url = "https://github.com/grpc-ecosystem/grpc-gateway.git"
default_branch = "main"
fork = false
head_sha = "e1c5f8deb96d0b3a1143959f40a09c71c543f07e"
html_url = "https://github.com/grpc-ecosystem/grpc-gateway"
stars = 19946
