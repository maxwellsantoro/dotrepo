schema = "dotrepo/v0.1"

[record]
mode = "overlay"
status = "verified"
source = "https://github.com/pgmq/pgmq"
generated_at = "2026-07-09T03:32:25.266275Z"

[record.trust]
confidence = "high"
provenance = [
    "imported",
    "inferred",
    "verified",
]
notes = "Bootstrapped from `.github/CODEOWNERS` and `pyproject.toml`. Filled `repo.name` and `repo.description` with inferred defaults. Imported `repo.toolchain.min` from `pyproject.toml`. This is an overlay bootstrap, not a maintainer-controlled canonical record. Resolved `repo.test` from `.github/workflows/client-tests.yml` after model escalation. Preserved prior verified status: no previously present field regressed in this refresh."

[repo]
name = "pgmq"
description = "A lightweight message queue. Like AWS SQS and RSMQ but on Postgres."
homepage = "https://pgmq.github.io/pgmq/"
license = "PostgreSQL"
visibility = "public"
languages = [
    "PLpgSQL",
    "Rust",
    "Python",
    "Makefile",
    "Shell",
    "Ruby",
]
test = "uv run pytest test.py -v"
topics = ["message-queue"]

[repo.toolchain]
min = "3.10"
ecosystem = "Python"

[owners]
maintainers = [
    "@ChuckHend",
    "@theory",
    "@v0idpwn",
    "@spencewenski",
]

[docs]
root = "https://pgmq.github.io/pgmq/"

[x.github]
archived = false
clone_url = "https://github.com/pgmq/pgmq.git"
default_branch = "main"
fork = false
head_sha = "e67a9c27037036c40f8793ca2966bf7d56484194"
html_url = "https://github.com/pgmq/pgmq"
stars = 5007
