schema = "dotrepo/v0.1"

[record]
mode = "overlay"
status = "inferred"
source = "https://github.com/pgmq/pgmq"
generated_at = "2026-07-06T18:56:42.714855Z"

[record.trust]
confidence = "medium"
provenance = [
    "imported",
    "inferred",
]
notes = "Bootstrapped from `.github/CODEOWNERS` and `pyproject.toml`. Filled `repo.name` and `repo.description` with inferred defaults. Left `repo.test` unset because `.github/workflows/client-tests.yml`, `.github/workflows/crate_ci.yml`, and `.github/workflows/extension_upgrade.yml` suggested conflicting test commands. Imported `repo.toolchain.min` from `pyproject.toml`. This is an overlay bootstrap, not a maintainer-controlled canonical record."

[repo]
name = "pgmq"
description = "A lightweight message queue. Like AWS SQS and RSMQ but on Postgres."
homepage = "https://pgmq.github.io/pgmq/"
license = "PostgreSQL"
visibility = "public"
languages = [
    "PLpgSQL",
    "Rust",
    "Python",
    "Makefile",
    "Shell",
    "Ruby",
]
topics = ["message-queue"]

[[repo.test_candidates]]
command = "uv run pytest test.py -v"
source = ".github/workflows/client-tests.yml"

[[repo.test_candidates]]
command = "make check.fmt"
source = ".github/workflows/crate_ci.yml"

[[repo.test_candidates]]
command = "UPGRADE_PHASE=pre uv run pytest upgrade_test.py -v"
source = ".github/workflows/extension_upgrade.yml"

[repo.toolchain]
min = "3.10"
ecosystem = "Python"

[owners]
maintainers = [
    "@ChuckHend",
    "@theory",
    "@v0idpwn",
    "@spencewenski",
]

[docs]
root = "https://pgmq.github.io/pgmq/"

[x.github]
archived = false
clone_url = "https://github.com/pgmq/pgmq.git"
default_branch = "main"
fork = false
head_sha = "80d273a399631f37a6a2b136a615dd673cd4e90a"
html_url = "https://github.com/pgmq/pgmq"
stars = 5003
