schema = "dotrepo/v0.1"

[record]
mode = "overlay"
status = "inferred"
source = "https://github.com/cli/cli"
generated_at = "2026-07-06T18:37:40.371777Z"

[record.trust]
confidence = "medium"
provenance = [
    "imported",
    "inferred",
]
notes = "Bootstrapped from `README.md`, `.github/CODEOWNERS`, `.github/SECURITY.md`, `.github/workflows/go.yml`, `Makefile`, and `go.mod`. Filled `repo.description` with inferred defaults. Maintainer information was imported from broad CODEOWNERS patterns; `owners.team` prefers `@cli/code-reviewers` from the repo-wide rule, and `owners.maintainers` preserves narrower owner candidates. SECURITY.md provided a policy or reporting URL rather than a direct mailbox, so `security_contact` preserves that URL. Inferred `repo.build` from `.github/workflows/go.yml`. Imported `repo.test` from `Makefile`. Imported `repo.toolchain.min` from `go.mod`. This is an overlay bootstrap, not a maintainer-controlled canonical record."

[repo]
name = "GitHub CLI"
description = "GitHub’s official command line tool"
homepage = "https://cli.github.com"
license = "MIT"
visibility = "public"
languages = [
    "Go",
    "Shell",
    "Makefile",
    "PowerShell",
    "Batchfile",
]
build = "go build -v ./cmd/gh"
test = "go test ./..."
topics = [
    "github-api-v4",
    "cli",
    "git",
    "golang",
]

[repo.toolchain]
min = "1.26.0"
ecosystem = "Go"

[owners]
maintainers = [
    "@cli/code-reviewers",
    "@cli/codespaces",
    "@cli/package-security",
    "@cli/tuf-root-reviewers",
    "@cli/skills",
]
team = "@cli/code-reviewers"
security_contact = "https://pkg.go.dev/golang.org/x/vuln/cmd/govulncheck"

[x.github]
archived = false
clone_url = "https://github.com/cli/cli.git"
default_branch = "trunk"
fork = false
head_sha = "27762fcfe431924c1a6c0baab994ef755ddc73dc"
html_url = "https://github.com/cli/cli"
stars = 45149
