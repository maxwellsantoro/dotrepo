schema = "dotrepo/v0.1"

[record]
mode = "overlay"
status = "verified"
source = "https://github.com/docker/cli"
generated_at = "2026-06-29T01:58:41.567232Z"

[record.trust]
confidence = "high"
provenance = [
    "imported",
    "inferred",
    "verified",
]
notes = "Bootstrapped from `README.md`, `.github/CODEOWNERS`, and `SECURITY.md`. Filled `repo.description` with inferred defaults. Maintainer information was imported from CODEOWNERS; `owners.team` is `@docker/runtime-owners` because it is the clearest imported team signal, but `owners.maintainers` still preserves narrower owner candidates. Left `repo.test` unset because `.github/workflows/e2e.yml` and `.github/workflows/test.yml` suggested conflicting test commands. This is an overlay bootstrap, not a maintainer-controlled canonical record. Auto-promoted to verified: all fields are honestly resolved."

[repo]
name = "Docker CLI"
description = "The Docker CLI"
homepage = "https://github.com/docker/cli"
license = "Apache-2.0"
visibility = "public"
languages = [
    "Dockerfile",
    "Go",
    "HCL",
    "Makefile",
    "Shell",
]
topics = [
    "docker",
    "cli",
]

[owners]
maintainers = [
    "@silvin-lubecki",
    "@docker/runtime-owners",
    "@albers",
    "@thaJeztah",
]
team = "@docker/runtime-owners"
security_contact = "security@docker.com"

[relations]
references = []

[x.github]
archived = false
clone_url = "https://github.com/docker/cli.git"
default_branch = "master"
fork = false
head_sha = "8900f1d330cb39e93e16d780a26bff1d7e07ba03"
html_url = "https://github.com/docker/cli"
stars = 5938
