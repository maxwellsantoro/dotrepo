schema = "dotrepo/v0.1"

[record]
mode = "overlay"
status = "imported"
source = "https://github.com/helm/helm"
generated_at = "2026-03-20T04:50:27.597988Z"

[record.trust]
confidence = "high"
provenance = ["imported"]
notes = "Imported from the upstream README, developer setup docs, CONTRIBUTING.md, SECURITY.md, OWNERS, and Makefile. `repo.build` and `repo.test` follow the official Helm developer workflow: build with `make` and run the default local checks with `make test`. `main` tracks Helm v4 development and is explicitly unstable; Helm v3 stable lives on `dev-v3`, so branch context matters when interpreting this overlay. Acceptance tests depend on the separate `helm/acceptance-testing` repository and are intentionally excluded from the default `repo.test`. Not maintainer-verified."

[repo]
name = "Helm"
description = "Helm is a tool for managing Charts. Charts are packages of pre-configured Kubernetes resources."
homepage = "https://helm.sh"
license = "Apache-2.0"
visibility = "public"
languages = [
    "go",
    "shell",
]
build = "make"
test = "make test"
topics = [
    "cncf",
    "chart",
    "kubernetes",
    "helm",
    "charts",
]

[owners]
maintainers = [
    "@banjoh",
    "@gjenkins8",
    "@joejulian",
    "@marckhouzam",
    "@mattfarina",
    "@robertsirc",
    "@sabre1041",
    "@scottrigby",
    "@technosophos",
    "@TerryHowe",
]
security_contact = "cncf-helm-security@lists.cncf.io"

[docs]
root = "https://helm.sh/docs/"
getting_started = "https://helm.sh/docs/intro/quickstart/"

[relations]
references = []

[x.github]
archived = false
clone_url = "https://github.com/helm/helm.git"
default_branch = "main"
fork = false
head_sha = "42f78ba60edf531d5161e00d9819a7c34d976343"
html_url = "https://github.com/helm/helm"
stars = 29597
